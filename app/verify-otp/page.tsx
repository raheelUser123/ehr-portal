import Link from 'next/link';export default function Page(){return <div className="auth-panel" style={{minHeight:'100vh'}}><div className="auth-box center"><h1>Verify your email</h1><p className="muted">Enter the 6-digit code sent to your email.</p><div className="row" style={{justifyContent:'center',margin:'22px 0'}}>{[1,2,3,4,5,6].map(i=><input key={i} className="input" style={{width:48,textAlign:'center',fontSize:20}} maxLength={1}/>)}</div><Link href="/dashboard" className="btn btn-primary" style={{display:'block'}}>Verify & continue</Link></div></div>}

"use client";import Link from "next/link";import {useState} from "react";
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);setErr('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const j=await r.json().catch(()=>({}));setLoading(false);if(!r.ok){setErr(j.error||'Login failed');return}location.href='/dashboard'}return <div className="authwrap"><div className="authvisual"><div className="brand"><div className="brandmark">+</div><div><h1 style={{fontSize:22,margin:0}}>EHR PORTAL</h1><small>Behavioral Health Management System</small></div></div><h1>Compassionate Care. Better Outcomes.</h1><p>A secure, modern resident documentation workspace for behavioral health facilities.</p></div><div className="authside"><div className="card authcard"><h2>Welcome Back</h2><div className="muted">Sign in to continue to EHR Portal</div>{err&&<div className="err" style={{marginTop:15}}>{err}</div>}<form onSubmit={submit}><div className="field"><label>Email Address</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div><div style={{display:'flex',justifyContent:'flex-end'}}><Link href="/forgot-password" style={{color:'#0b8da2',fontSize:13}}>Forgot password?</Link></div><button className="btn btn-primary" disabled={loading}>{loading?'Signing in...':'Sign In'}</button></form><div style={{textAlign:'center',marginTop:18,fontSize:13}}>Don't have an account? <Link href="/signup" style={{color:'#0b8da2',fontWeight:800}}>Sign up</Link></div></div></div></div>}

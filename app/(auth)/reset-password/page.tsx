"use client";import {useEffect,useState} from "react";export default function Reset(){const [email,setEmail]=useState('');const [otp,setOtp]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');useEffect(()=>{setEmail(new URLSearchParams(window.location.search).get('email')||'')},[]);async function submit(e:any){e.preventDefault();const r=await fetch('/api/auth/reset',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,otp,password})});const j=await r.json();if(!r.ok){setMsg(j.error);return}location.href='/login'}return <div className="authside" style={{minHeight:'100vh'}}><div className="card authcard"><h2>Reset Password</h2>{msg&&<div className="err">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Reset Code</label><input className="input" value={otp} onChange={e=>setOtp(e.target.value)} required/></div><div className="field"><label>New Password</label><input className="input" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary">Update Password</button></form></div></div>}
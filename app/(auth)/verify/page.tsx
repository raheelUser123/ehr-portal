"use client";import {useEffect,useState} from "react";export default function Verify(){const [email,setEmail]=useState('');const [otp,setOtp]=useState(''),[msg,setMsg]=useState('');useEffect(()=>{setEmail(new URLSearchParams(window.location.search).get('email')||'')},[]);async function submit(e:any){e.preventDefault();const r=await fetch('/api/auth/verify',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,otp})});const j=await r.json();if(!r.ok){setMsg(j.error);return}location.href='/dashboard'}return <div className="authside" style={{minHeight:'100vh'}}><div className="card authcard"><h2>Verify Email</h2><p className="muted">Enter the 6-digit verification code sent to {email}. Configure the Supabase confirmation email template to include the token.</p>{msg&&<div className="err">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Verification Code</label><input className="input" inputMode="numeric" maxLength={6} value={otp} onChange={e=>setOtp(e.target.value)} required/></div><button className="btn btn-primary">Verify & Continue</button></form></div></div>}

"use client";import {useState} from "react";export default function Forgot(){const [email,setEmail]=useState(''),[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const r=await fetch('/api/auth/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});await r.json();setMsg('If the account exists, a reset code has been sent.');setTimeout(()=>location.href=`/reset-password?email=${encodeURIComponent(email)}`,800)}return <div className="authside" style={{minHeight:'100vh'}}><div className="card authcard"><h2>Forgot Password</h2>{msg&&<div className="toast">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Email Address</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="btn btn-primary">Send Reset Code</button></form></div></div>}

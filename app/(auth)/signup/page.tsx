"use client";import Link from "next/link";import {useState} from "react";export default function Signup(){const [form,setForm]=useState({name:'',email:'',password:''}),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok){setMsg(j.error);return}location.href=`/verify?email=${encodeURIComponent(form.email)}`};return <div className="authwrap"><div className="authvisual"><h1>Create your secure EHR workspace.</h1><p>Account verification is handled by email OTP.</p></div><div className="authside"><div className="card authcard"><h2>Create Account</h2>{msg&&<div className="err">{msg}</div>}<form onSubmit={go}><div className="field"><label>Full Name</label><input className="input" required onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>Email</label><input className="input" type="email" required onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field"><label>Password</label><input className="input" type="password" minLength={8} required onChange={e=>setForm({...form,password:e.target.value})}/></div><button className="btn btn-primary">Create Account</button></form><div style={{textAlign:'center',marginTop:16}}><Link href="/login">Back to login</Link></div></div></div></div>}

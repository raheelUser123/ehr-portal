import {notFound} from "next/navigation";import EmployeeRecordManager from "@/components/EmployeeRecordManager";import {getEmployeeModule} from "@/lib/employee-modules";import {getSession,hasCapability} from "@/lib/auth";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const module=getEmployeeModule(slug);if(!module)notFound();const s=await getSession();return <EmployeeRecordManager module={module} canManage={hasCapability(s,"employee.manage")}/>}

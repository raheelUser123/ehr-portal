import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";import {getSession} from "@/lib/auth";import {isAdminRole} from "@/lib/admin";export async function DELETE(_:Request,c:{params:Promise<{id:string}>}){const s=await getSession();if(!s||!isAdminRole(s.role))return NextResponse.json({error:'Forbidden'},{status:403});const {id}=await c.params;const a=createAdminClient();const {error}=await a.from('facilities').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({success:true})}

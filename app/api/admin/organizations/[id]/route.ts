import { NextResponse } from "next/server";import { createAdminClient } from "@/lib/supabase/admin";import { getSession,hasCapability } from "@/lib/auth";
export async function PATCH(req:Request,c:{params:Promise<{id:string}>}){const s=await getSession();if(!s||!hasCapability(s,"admin.organizations"))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await c.params;const b=await req.json();const a=createAdminClient();const {data,error}=await a.from("organizations").update({name:b.name}).eq("id",id).select("*").single();if(error)return NextResponse.json({error:error.message},{status:400});await a.from("audit_logs").insert({actor_id:s.id,entity_type:'organization',entity_id:id,action:'UPDATE_ORGANIZATION'});return NextResponse.json(data)}
export async function DELETE(_:Request,c:{params:Promise<{id:string}>}){const s=await getSession();if(!s||!hasCapability(s,"admin.organizations"))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await c.params;const a=createAdminClient();const {error}=await a.from("organizations").delete().eq("id",id);if(error)return NextResponse.json({error:error.message},{status:400});await a.from("audit_logs").insert({actor_id:s.id,entity_type:'organization',entity_id:id,action:'DELETE_ORGANIZATION'});return NextResponse.json({success:true})}

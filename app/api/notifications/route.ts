import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";import {requireCapability} from "@/lib/auth";
const out=(n:any)=>({...n,userId:n.user_id,createdAt:n.created_at});
export async function GET(){const s=await requireCapability("notifications.view");const db=createAdminClient();const {data,error}=await db.from("notifications").select("*").eq("user_id",s.id).order("created_at",{ascending:false}).limit(50);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json((data||[]).map(out))}
export async function PUT(){const s=await requireCapability("notifications.view");const db=createAdminClient();const {error}=await db.from("notifications").update({read:true}).eq("user_id",s.id).eq("read",false);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}

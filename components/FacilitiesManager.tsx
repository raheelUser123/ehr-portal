"use client";
import {useState} from "react";
export default function FacilitiesManager({initialFacilities,organizations}:any){const [rows,setRows]=useState(initialFacilities);const [form,setForm]=useState({name:"",address:"",organization_id:""});const [msg,setMsg]=useState("");async function add(e:any){e.preventDefault();const r=await fetch('/api/admin/facilities',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)return setMsg(j.error||'Unable to create facility');setRows([j,...rows]);setForm({name:"",address:"",organization_id:""});setMsg('Facility created.')}async function del(id:string){if(!confirm('Delete this facility?'))return;const r=await fetch('/api/admin/facilities/'+id,{method:'DELETE'});if(r.ok)setRows(rows.filter((x:any)=>x.id!==id))}return <><div className="pagehead"><div><h2>Facilities</h2><div className="muted">Manage behavioral health facility locations.</div></div></div>{msg&&<div className="toast" style={{marginBottom:14}}>{msg}</div>}<form className="card section" onSubmit={add}><h3>Add Facility</h3><div className="formgrid"><div className="field"><label>Facility Name</label><input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>Organization</label><select className="select" required value={form.organization_id} onChange={e=>setForm({...form,organization_id:e.target.value})}><option value="">Select organization</option>{organizations.map((o:any)=><option key={o.id} value={o.id}>{o.name}</option>)}</select></div><div className="field full"><label>Address</label><input className="input" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></div></div><button className="btn btn-primary" style={{marginTop:14}}>Add Facility</button></form><div className="card section" style={{marginTop:16}}><div className="tablewrap"><table className="table"><thead><tr><th>Facility</th><th>Organization</th><th>Address</th><th>Action</th></tr></thead><tbody>{rows.map((f:any)=><tr key={f.id}><td><b>{f.name}</b></td><td>{organizations.find((o:any)=>o.id===f.organization_id)?.name||'—'}</td><td>{f.address||'—'}</td><td><button className="btn btn-danger" onClick={()=>del(f.id)}>Delete</button></td></tr>)}</tbody></table></div></div></>}
